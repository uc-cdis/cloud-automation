apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datadir-influxdb
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: standard
