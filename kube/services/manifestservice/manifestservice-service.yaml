kind: Service
apiVersion: v1
metadata:
  name: manifestservice-service
spec:
  selector:
    app: manifestservice
    release: production
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: https
  type: ClusterIP
