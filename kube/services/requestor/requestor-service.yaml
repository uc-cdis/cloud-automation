kind: Service
apiVersion: v1
metadata:
  name: requestor-service
  annotations:
    getambassador.io/config: | # TODO do i need this?
      ---
      apiVersion: ambassador/v1
      ambassador_id: "gen3"
      kind:  Mapping
      name:  requestor_mapping
      prefix: /index/
      service: http://requestor-service:80
spec:
  selector:
    app: requestor
    release: production
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 
      name: http
  type: ClusterIP

