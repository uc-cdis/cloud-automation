apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariner-nfs-pvc
spec:
  storageClassName: ""
  volumeName: mariner-nfs-pv
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10GB