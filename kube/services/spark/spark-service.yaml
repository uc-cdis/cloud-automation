apiVersion: v1
kind: Service
metadata:
  name: namenode
spec:
  ports:
    - port: 9000
      targetPort: 9000
  clusterIP: None
  selector:
    app: namenode

---

apiVersion: v1
kind: Service
metadata:
  name: datanode
spec:
  ports:
    - port: 50010
      targetPort: 50010
  clusterIP: None
  selector:
    app: datanode

---

apiVersion: v1
kind: Service
metadata:
  name: resourcemanager
spec:
  ports:
    - port: 8088
      targetPort: 8088
  clusterIP: None
  selector:
    app: resourcemanager

---

apiVersion: v1
kind: Service
metadata:
  name: nodemanager
spec:
  ports:
    - port: 8042
      targetPort: 8042
  clusterIP: None
  selector:
    app: nodemanager

---

apiVersion: v1
kind: Service
metadata:
  name: sparkmaster
spec:
  ports:
    - name: p7077
      port: 7077
      targetPort: 7077
    - name: p8080
      port: 8080
      targetPort: 8080
  clusterIP: None
  selector:
    app: spark-master

---

apiVersion: v1
kind: Service
metadata:
  name: sparkworker
spec:
  ports:
    - port: 8081
      targetPort: 8081
  clusterIP: None
  selector:
    app: spark-worker