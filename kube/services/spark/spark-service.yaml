apiVersion: v1
kind: Service
metadata:
  name: namenode
spec:
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      name: hdfs
  clusterIP: None
  selector:
    app: namenode

---

apiVersion: v1
kind: Service
metadata:
  name: datanode
spec:
  ports:
    - protocol: TCP
      port: 50010
      targetPort: 50010
      name: data
  clusterIP: None
  selector:
    app: datanode

---

apiVersion: v1
kind: Service
metadata:
  name: resourcemanager
spec:
  ports:
    - protocol: TCP
      port: 8088
      targetPort: 8088
      name: resource
  clusterIP: None
  selector:
    app: resourcemanager

---

apiVersion: v1
kind: Service
metadata:
  name: nodemanager
spec:
  ports:
    - protocol: TCP
      port: 8042
      targetPort: 8042
      name: node
  clusterIP: None
  selector:
    app: nodemanager

---

apiVersion: v1
kind: Service
metadata:
  name: spark-master
spec:
  ports:
  - name: web-ui
    protocol: TCP
    port: 8080
    targetPort: 8080
  - name: master
    protocol: TCP
    port: 7077
    targetPort: 7077
  - name: master-rest
    protocol: TCP
    port: 6066
    targetPort: 6066
  clusterIP: None
  selector:
    app: spark-master

---

apiVersion: v1
kind: Service
metadata:
  name: spark-worker
spec:
  ports:
    - name: web-ui
      protocol: TCP
      port: 8081
      targetPort: 8081
  clusterIP: None
  selector:
    app: spark-worker