kind: Service
apiVersion: v1
metadata:
  name: s3listener-service
spec:
  selector:
    app: s3listener
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
