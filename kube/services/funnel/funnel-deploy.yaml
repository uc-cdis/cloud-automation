# The funnel service is deployed via helm by `gen3 kube-setup-gen3-workflow`.
# This file is only used by `gen3 kube-setup-networkpolicy service funnel` to configure network
# policies.

metadata:
  annotations:
    gen3.io/network-ingress: "gen3-workflow"
