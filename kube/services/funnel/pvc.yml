apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: funnel-storage-pvc
spec:
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
