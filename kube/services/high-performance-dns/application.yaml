apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: high-performance-dns-application
  namespace: argocd
spec:
  destination:
    namespace: kube-system
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/uc-cdis/cloud-automation.git
    targetRevision: master
    path: kube/services/high-performance-dns
    directory:
      exclude: "application.yaml"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
