apiVersion: apps/v1
kind: Deployment
metadata:
  name: gen3-discovery-ai-deployment
spec:
  selector:
    # Only select pods based on the 'app' label
    matchLabels:
      app: gen3-discovery-ai
      release: production
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: gen3-discovery-ai
        release: production
        GEN3_DATE_LABEL
    spec:
      volumes:
        - name: gen3-discovery-ai-g3auto-volume
          secret:
            secretName: gen3-discovery-ai-g3auto
        - name: gen3-discovery-ai-knowledge-library-volume
          configMap:
            name: gen3-discovery-ai-knowledge-library
      containers:
      - name: gen3-discovery-ai
        GEN3_GEN3-DISCOVERY-AI_IMAGE
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
          - name: GEN3_DEBUG
            GEN3_DEBUG_FLAG|-value: "False"-|
        volumeMounts:
          - name: gen3-discovery-ai-g3auto-volume
            readOnly: true
            mountPath: /.env
            subPath: env
          - name: gen3-discovery-ai-knowledge-library-volume
            readOnly: false
            mountPath: /knowledge
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 1
          limits:
            cpu: 2
            memory: 512Mi
