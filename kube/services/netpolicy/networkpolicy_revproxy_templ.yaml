apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: networkpolicy-revproxy
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: revproxy
  ingress:
    - from:
      - podSelector:
          matchLabels:
            app: revproxy
      - podSelector:
          matchLabels:
             app: portal
      - ipBlock:
           cidr: 0.0.0.0/0
      ports:
         - port: 80
         - port: 443
  egress:
    - to:
      - podSelector:
          matchLabels:
            app: revproxy
      - podSelector:
          matchLabels:
             app: indexd
      - podSelector:
          matchLabels:
             app: peregrine
      - podSelector:
          matchLabels:
             app: sheepdog
      - podSelector:
          matchLabels:
             app: portal
      - podSelector:
          matchLabels:
             app: fence
      - ipBlock:
           cidr: cloudproxy_ip/32
      - ipBlock:
           cidr: 54.224.0.0/12
      - ipBlock:
           cidr: 52.92.16.0/20
  policyTypes:
   - Ingress
   - Egress
