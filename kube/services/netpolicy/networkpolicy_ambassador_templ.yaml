apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: networkpolicy-ambassador
spec:
  podSelector:
    matchLabels:
      service: ambassador
  ingress:
    - {}
  egress:
    - {}

  policyTypes:
   - Ingress
   - Egress
