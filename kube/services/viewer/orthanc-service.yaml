kind: Service
apiVersion: v1
metadata:
  name: ohif-viewer-service
spec:
  selector:
    app: orthanc
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: null
      name: http
  type: ClusterIP