          location / {
              if ($csrf_check !~ ^ok-\S.+$) {
                return 403 "failed csrf check";
              }
              rewrite ^/(.*)$ /dashboard/Public/index.html redirect;
          }
