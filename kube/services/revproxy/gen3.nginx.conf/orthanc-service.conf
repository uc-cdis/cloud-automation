location /orthanc/v1/ {
    if ($csrf_check !~ ^ok-\S.+$) {
      return 403 "failed csrf check";
    }

    set $proxy_service  "orthanc";
    set $upstream http://localhost:8080/lw-workspace/proxy.$namespace.svc.cluster.local;
    rewrite ^/orthanc/v1/(.*) /$1 break;
    proxy_pass $upstream;
}
