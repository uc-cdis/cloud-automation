
- name: daemon reload
  systemd:
    daemon_reload: yes
    name: docker
    #state: restarted

- name: restart docker
  systemd:
    state: restarted
    name: docker

- name: apt update
  apt:
    update_cache: yes
